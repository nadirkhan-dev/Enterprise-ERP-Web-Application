Run the project test suite using `npm test` (Vitest).

1. Execute `npm test` and capture the output
2. Report a pass/fail summary:
   - Total scenarios (describe blocks) and assertions
   - Number passed vs failed
3. For any failures, show:
   - The failing scenario name and test description
   - Expected vs received values
   - File path and line number
4. Suggest fixes for failing tests based on the error output
