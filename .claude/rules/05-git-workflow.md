# Git Policy

**All git operations are handled manually by the user.** Do NOT perform any of the following:
- Creating branches
- Staging or committing files
- Pushing to remotes
- Creating pull requests
- Running `git` commands of any kind

When changes are ready, describe what was modified so the user can commit appropriately.
